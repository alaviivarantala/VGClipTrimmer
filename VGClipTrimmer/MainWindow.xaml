<adonisControls:AdonisWindow x:Class="VGClipTrimmer.MainWindow"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             Title="Game Highlight Clipper - 0.0.1 alpha"
                             TitleBarBackground="{StaticResource BluePurpleBrush}"
                             WindowButtonHighlightBrush="{StaticResource HighlightBrush}">

    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>

    <adonisControls:AdonisWindow.TitleBarContent>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="🎺"
                    Style="{DynamicResource {x:Static adonisUi:Styles.WindowButton}}"
                    
                    FontFamily="Segoe UI"
                    FontSize="14"
                    ToolTip="Send feedback"/>
        </StackPanel>
    </adonisControls:AdonisWindow.TitleBarContent>

    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBox Text="Enter video file path..." Width="250" />
        <Button Content="📂" Width="35" Height="25" Margin="8 0"/>
        <Button Content="🔎" Width="35" Height="25" />
    </StackPanel>
    
</adonisControls:AdonisWindow>